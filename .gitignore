node_modules/
dist/
out/
assets/
coverage/
*.log
*.swp
*.tsbuildinfo
.env
.keys
*.keys
.DS_Store
.idea/
.vscode/

# e2e render output (vhs gifs/logs) — regenerated each run.
test/e2e/out/
# e2e staging binary — a compiled ccc-morph copied in before the image build.
test/e2e/ccc-morph

# e2e notes store written during the notes tapes (regenerated). Match both the
# directory (notes.tape) and the file form that notes-add-failure.tape creates to
# force a store-write failure.
test/e2e/xdg/ccc-morph/notes
